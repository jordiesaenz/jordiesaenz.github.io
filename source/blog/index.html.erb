---

title: Jordie Saenz | Blog
id: blog
header: "Blog."
tagline: "This is where I write stuff."
page_name: Blog
pageable: true

---
<div class="blog-listing">
  <% blog.articles[0...10].each do |article| %>
    <a class="blog-card-link" href="<%= article.url %>">
      <div class="blog-card">
        <time class="date"><%= article.date.strftime('%x') %></time>
        <div class="grid">
          <div class="col-xl-1-6 col-md-1-4 col-sm-1-3">
            <img class="poster-image" src="<%= article.data.image_sq %>">
          </div>
          <div class="col-xl-5-6 col-md-3-4 col-sm-2-3">
            <h3 class="post-title"><%= article.title %></h3>
            <div class="post-body">
              <%= article.data.summary %>
            </div>
          </div>
        </div>
      </div>
    </a>
  <% end %>
</div>
